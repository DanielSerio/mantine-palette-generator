import React from 'react';
import { Container, Text } from '@mantine/core';

export default function AppFooter() {
  return (
    <footer>
      <Container mt={200}>
        <Text color={'gray'}>Created By Dan Serio</Text>
      </Container>
    </footer>
  );
}
